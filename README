Данная программа, из имеющегося массива строк формирует новый массив из строк, длина которых меньше, либо равна 3 символам. Первоначальный массив задается на старте выполнения алгоритма.

(1) Создается первичный массив строк (someArray) и пустой массив для копирования (newArrayOfStringsTreeOrLess) 
(2) При помощи цикла forEach проходим по элементам массива с проверкой колличестава длинны строк (<=3) и записываем нужные строки в массив  (newArrayOfStringsTreeOrLess) 
(3) Выводим новый массив на экран

Примеры:
[“Hello”, “2”, “world”, “:-)”] → [“2”, “:-)”]
[“1234”, “1567”, “-2”, “computer science”] → [“-2”]
[“Russia”, “Denmark”, “Kazan”] → []